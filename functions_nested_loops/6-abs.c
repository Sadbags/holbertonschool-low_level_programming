#include "main.h"
/**
 * write a funtion that computes the absolute value of an integer
 * @n: The integer to be printed
 * 
 * Return: void
 */
int _abs(int n)
{
if (n <= 0)
return (n);
else 
return (-n);
}
